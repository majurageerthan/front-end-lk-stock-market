import styles from './styles.module.css';

const LoadingAnimationCenter = () => (
  <div className={styles.loaderContainer}>
    <div className={styles.loader} />
  </div>

);

export default LoadingAnimationCenter;
